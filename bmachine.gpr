project Bmachine is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("main.adb");

   package Compiler is
      for Switches ("ada") use ("-gnato", "-gnatp", "-fstack-check");
   end Compiler;

end Bmachine;

